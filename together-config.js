// Leave empty when the Node server hosts the site. For a static frontend,
// set the public HTTPS origin of together-server.js (never put credentials here).
window.DuckTogetherConfig = { serverUrl: '' };
